lifecycle_manager:
  ros__parameters:
    # managing_node_list: ["acquisition_left", "inference", "saltas", "velocity_estimation", "slam", "path_planning"]
    managing_node_list: ["inference", "saltas", "velocity_estimation", "mpc", "path_planning", "slam"]
    heartbeat_timeout_period: 1000  #heartbeat period in milliseconds
    heartbeat_frequency:  5 # in Hz
    resurrect_failed_nodes: false
    heartbeat_failures_before_resurrection: 3
    
    # managing_node_list: ["acquisition_left", "acquisition_right", 
    # "inference", "slam", "velocity_estimation", "saltas", "mpc", 
    # "path_planning", "pure_pursuit"]
  
  # The managing node list parameter should have all the nodes that are managed by the manager.
  # Whichever node is not needed in the end due to a specific mission happening is handled inside
  # the manager itself.